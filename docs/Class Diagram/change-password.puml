@startuml

skinparam classAttributeIconSize 0
skinparam linetype polyline

class AccountController <<controller>> {
    -accountService : AccountService

    +changePassword(@PathVariable Long userId, @RequestBody ApiRequests.PasswordChange request) : Object
}

class AccountService <<service>> {
    -support : DomainSupportService
    -passwordEncoder : BCryptPasswordEncoder

    +changePassword(userId : Long, request : ApiRequests.PasswordChange) : Map<String, Object>
    -validatePassword(password : String, confirmPassword : String) : void
    -revokeAllTokens(user : AppUser) : void
}

class BCryptPasswordEncoder <<service>> {
    +matches(rawPassword : CharSequence, encodedPassword : String) : boolean
    +encode(rawPassword : CharSequence) : String
}

class DomainSupportService <<service>> {
    -userRepository : AppUserRepository

    +requireText(value : String, message : String) : void
    +getUser(userId : Long) : AppUser
    +badRequest(message : String) : ApiException
    +userSummary(user : AppUser) : Map<String, Object>
}

class "ApiRequests.PasswordChange" as PasswordChangeDTO <<dto>> {
    -currentPassword : String
    -newPassword : String
    -confirmPassword : String
}

interface AppUserRepository <<repository>> {
    +findById(id : Long) : Optional<AppUser>
    +save(entity : AppUser) : AppUser
}

class AuditEntity <<entity>> {
    -createdAt : LocalDateTime
    -updatedAt : LocalDateTime
}

class AppUser <<entity>> {
    -userId : Long
    -fullName : String
    -email : String
    -phone : String
    -passwordHash : String
    -avatarUrl : String
    -dateOfBirth : LocalDate
    -address : String
    -status : AccountStatus
    -accountLocked : boolean
    -lockReason : String
    -lastLoginAt : LocalDateTime
    -emailVerified : boolean
    -emailVerificationToken : String
    -emailVerificationSentAt : LocalDateTime
    -passwordResetToken : String
    -passwordResetSentAt : LocalDateTime
}

' Relationships
AccountController ..> AccountService
AccountController ..> PasswordChangeDTO
AccountService ..> PasswordChangeDTO
AccountService ..> DomainSupportService
AccountService ..> BCryptPasswordEncoder
DomainSupportService ..> AppUserRepository
DomainSupportService ..> AppUser
AppUserRepository --> AppUser
AppUser -|> AuditEntity

@enduml
